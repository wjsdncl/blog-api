services:
  - type: web
    name: blog-api
    runtime: node
    buildCommand: npm ci && npx prisma generate && npx prisma migrate deploy && tsc && npx tsc-alias
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
